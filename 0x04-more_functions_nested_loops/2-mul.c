#include "main.h"

/**
 * mul - function multiplying two integers
 * @a: first user input
 * @b: second user input
 * Return: multiplication result of two inputs
 */

int mul(int a, int b)
{
	return (a * b);
}
